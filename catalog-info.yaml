# Gerado por fernando-moretes/platform. Não edite aqui: mude no orquestrador e
# rode `make rollout`. É o que o Backstage (backstage.home.lab) lê deste repo.
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ref-rs-visao-real
  description: "Public Fernando Moretes repository connected to the broader architecture and engineering portfolio."
  annotations:
    github.com/project-slug: fernandofatech/ref-rs-visao-real
    # URL da arvore com barra final: e dela que o TechDocs resolve `dir:.`;
    # sem a barra ele sobe um nivel e pede api.github.com/repos/<dono> (404).
    backstage.io/source-location: url:https://github.com/fernandofatech/ref-rs-visao-real/tree/main/
    sonarqube.org/project-key: ref-rs-visao-real
    # TechDocs: mkdocs.yml + docs/ do proprio repositorio (rolados pelo platform)
    backstage.io/techdocs-ref: dir:.
  links:
    - url: https://github.com/fernandofatech/ref-rs-visao-real/actions
      title: Actions
      icon: github
    - url: https://sonarqube.home.lab/dashboard?id=ref-rs-visao-real
      title: SonarQube
    - url: https://defectdojo.home.lab/product?name=ref-rs-visao-real
      title: DefectDojo
  # tags so aceitam [a-z0-9-]: ci-python.yml (ci-node.yml) tem ponto e reprovava
  # todo componente na policy check do Backstage.
  tags: [documentation, python]
spec:
  type: documentation
  lifecycle: experimental
  owner: group:plataforma
  system: homelab
